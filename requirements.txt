# Local dataset-prep dependencies (used by scripts/build_datasets.py, already run)
datasets>=2.19.0
huggingface_hub>=0.23.0
pandas>=2.0.0

# Training/inference dependencies - install these on the GPU runtime
# (e.g. Google Colab) that runs the notebooks and src/inference.py.
unsloth
torch>=2.1.0
transformers>=4.40.0
trl>=0.9.0
peft>=0.10.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
sentencepiece
